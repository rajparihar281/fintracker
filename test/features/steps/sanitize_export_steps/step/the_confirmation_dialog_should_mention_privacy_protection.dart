import 'package:flutter_test/flutter_test.dart';\n\nFuture<void> theConfirmationDialogShouldMentionPrivacyProtection(WidgetTester tester) async {\n  // Verify the confirmation dialog contains privacy protection message\n  expect(find.textContaining('Personal information will be removed for privacy'), findsOneWidget);\n  \n  // Verify it's different from the standard message\n  expect(find.text('Want to export all the data to a file?'), findsNothing);\n}